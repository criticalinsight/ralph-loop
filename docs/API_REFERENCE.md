# API Reference

This document outlines the API endpoints available in the Ralph application.

## Base URL
Local development: `http://localhost:8000`

## Endpoints

### 1. General Info
**Endpoint:** `GET /info`

**Description:**
Returns the operational status and metadata of the agent service.

**Response Schema (JSON):**